#include "../../include/minishell.h"

// t_lexer	*init_lexer(char *input)
// {
// 	t_lexer	*lexer;

// 	lexer = malloc(sizeof(t_lexer));
// 	if (!lexer)
// 		return (NULL);
// 	ft_bzero(lexer->input, 0);
// 	return (lexer);
// }
